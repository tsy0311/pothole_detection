# requirements
# Usage: pip install -r requirements.txt

# Base ----------------------------------------
ultralytics>=8.3.233
opencv-python>=4.12.0
pillow>=12.0.0
requests>=2.32.0

# Data Science & Visualization ----------------------------------------
numpy>=2.2.6
pandas>=2.3.3
matplotlib>=3.10.7
seaborn>=0.13.2
pyyaml>=6.0.3

# Deep Learning (installed as ultralytics dependencies) -----------------
# torch>=2.2.2
# torchvision>=0.17.2
# scipy>=1.16.3

# Note: tkinter comes with Python, no need to install separately
# For systems without tkinter, install python3-tk (Linux) or use Python installer with tkinter (Windows/macOS)

# Note: NumPy 2.x Compatibility
# PyTorch 2.2.2 shows warnings with NumPy 2.x but still functions.
# For full compatibility, you can downgrade NumPy:
#   pip install --user --break-system-packages "numpy<2.0" --force-reinstall
# The notebook (model.ipynb) includes automatic NumPy version management.
# All packages are at their latest versions as of the last update.

# PDF Processing ----------------------------------------
PyPDF2>=3.0.1