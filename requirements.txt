# requirements
# Usage: pip install -r requirements.txt

# Base ----------------------------------------
ultralytics>=8.0.0
opencv-python>=4.6.0
pillow>=9.0.0
requests>=2.28.0

# Note: tkinter comes with Python, no need to install separately
# For systems without tkinter, install python3-tk (Linux) or use Python installer with tkinter (Windows/macOS)